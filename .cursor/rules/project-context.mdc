---
description: ERP 報價助手專案背景、現況技術棧與未來架構方向
alwaysApply: true
---

# ERP 報價快速打單助手

輕量化報價單管理系統：離線自動存檔、品項動態編輯、即時金額計算、Excel 匯出與 PDF/列印。

## 現況技術棧（以此為準開發）

- **前端**：React 19 + TypeScript + Vite 6
- **樣式**：Tailwind CSS v4（`@tailwindcss/vite`）
- **工具庫**：SheetJS (`xlsx`)、Lucide React、Motion
- **狀態管理**：React 元件內 state（`src/App.tsx`），尚未使用 Zustand
- **開發**：`nvm use` → `npm install` → `npm run dev`（port 3000，需 Node.js >= 20，見 `.nvmrc`）

## 關鍵目錄

- `src/App.tsx` — 主頁面與狀態管理
- `src/components/` — UI 元件
- `src/utils/` — Excel 匯出、預設資料等工具
- `src/types.ts` — 全域 TypeScript 型別
- `backend/` — 凌越 SOAP **唯讀**代理 API（`npm run dev:api`）
- `scripts/ly_erp_probe.py` — 凌越連線探測腳本

## 現況與未來架構

- **現況**：React SPA + `localStorage`；凌越整合為 **唯讀**（貨品搜尋 → 品項快選／品名自動完成）
- **開發**：`npm run dev:api`（後端 :8000）+ `npm run dev`（前端 :3000）；凌越憑證在根目錄 `.env`
- **規劃中**：PostgreSQL、帳號角色、凌越 **寫入**推送（Phase 4，須 Noah 同意）
- **ERP 整合準則**：見 `.cursor/rules/erp-integration-architecture.mdc`（現階段禁止 `LyDataIn` 等寫入）
- 不得擅自引入 Next.js、Zustand；擴充 PG 或凌越寫入前須經 Noah 同意並提供移轉計畫
- 後端 Python 環境以 **uv** 管理（`backend/` 內 `uv sync` / `uv run`）

## AI 作業原則

- 產出可實際運作的完整邏輯，不使用 `TODO` 或 placeholder
- 修改範圍最小化，不更動與任務無關的程式碼
