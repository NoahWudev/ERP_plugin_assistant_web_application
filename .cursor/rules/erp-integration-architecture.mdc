---
description: 報價外掛與凌越 ERP 整合架構、資料歸屬與實作節奏（開發準則）
alwaysApply: true
---

# 報價外掛 × 凌越 ERP 整合架構

## 定位

- 外掛是業務的**前置打單工作台**；凌越是**正式單據與主檔的權威來源（Source of Truth）**
- 日常操作走外掛 + PostgreSQL（規劃中；現階段報價資料仍以 localStorage 為主）
- **長期目標**：確認收單（`ACCEPTED`）後才透過凌越 SOAP API 推送；現階段**尚未實作推送**
- `ACCEPTED`＝客戶（或其業務經理）確認成交／簽回，與 UI「客戶簽回結案」為同一業務動作
- 目標：減少業務直接使用凌越的時間，同時不污染凌越未成交資料

## 現階段合作範圍（唯讀，以此為準）

**目前只從凌越讀取資料，不做任何寫入。**

| 允許 | 禁止（未經 Noah 明確同意前不得實作） |
|------|--------------------------------------|
| `LyGetPassKey` 取金鑰 | `LyDataIn` / `LyNewDataIn` 轉入寫入 |
| `LyDataOut` / `LyDataPage` 轉出查詢 | `LyDataDel` 刪除 |
| 後端代理搜尋主檔（如 `000000` 貨品） | `ACCEPTED` 觸發推送客戶／貨品／報價單 |
| 前端品項快選、品名自動完成（唯讀） | 直連凌越 DB 寫入 |

- 凌越 SOAP 服務位址：`ErpService.svc`（`IErpService`）；憑證放根目錄 `.env`，**勿**加 `VITE_` 前綴
- 已落地範例：`backend/` 貨品搜尋 API、`scripts/ly_erp_probe.py` 連線探測
- 貨品主檔搜尋**不帶單價**（報價單價不在 `000000`）；單價由使用者在報價列自行填寫

## 技術架構

```
React 前端 → Python 後端 API（backend/）→ 凌越 ERP（SOAP，現階段僅唯讀）
                ↓
         localStorage / 未來 PostgreSQL（外掛專屬資料）
```

- 前端**不直接**連凌越
- 後端：`backend/`，Python 以 **uv** 管理；開發：`npm run dev:api`（:8000）+ `npm run dev`（:3000，Vite proxy `/api`）
- 擴充**寫入**凌越或引入 PostgreSQL 遷移前，須經 Noah 同意並提供移轉計畫

## 資料歸屬

**只存外掛（現為 localStorage，未來 PostgreSQL）：** 草稿、流程狀態、KPI、外掛暫用單號、凌越沒有的自訂欄位、報價單價

**現階段唯讀自凌越：** 貨品主檔（`000000`）等主檔查詢；未來可擴充客戶（`00000D`）等

**長期（尚未實作）：** 外掛先行建立，收單才推送凌越——新客戶、新貨品、報價單（`ACCEPTED`）

**凌越為權威、外掛快取（規劃）：** 既有客戶／貨品主檔、正式單號與 `erp_id`

## 欄位分類（設計時必標）

每個欄位／資料源須標註以下三類之一，避免讀寫路徑混亂：

| 分類 | 儲存 | 讀取（現階段） | 寫入凌越（長期） |
|------|------|----------------|------------------|
| `plugin_only` | 僅外掛 DB / localStorage | 查外掛 | 永不推送 |
| `erp_cached` | 外掛快取，凌越為準 | **後端 SOAP 唯讀**查凌越（或快取 PG） | `ACCEPTED` 才推送（**暫不實作**） |
| `erp_live` | 不持久化（或極短 TTL）；凌越為準 | **後端即時** SOAP 唯讀 | 只讀；關聯單據推送留待 Phase 4 |

**範例**

- `plugin_only`：報價流程狀態、KPI、外掛備註、報價單價、凌越無對應欄位
- `erp_cached`：客戶／貨品主檔（品號、名稱、規格、單位等）；**現已用於貨品搜尋**
- `erp_live`：庫存、即時牌價、下單前須當下正確的 ERP 欄位

**即時讀取**：後端呼叫凌越 SOAP，前端只打 Python API，不直連凌越。

## 必守規則

1. **現階段凌越整合僅唯讀**；不得新增 `LyDataIn` 等寫入呼叫或推送流程
2. `erp_cached` / `erp_live` 主檔由後端 SOAP 讀取；前端不直連凌越
3. 報價編輯寫入 localStorage（未來 PG）；`plugin_only` 永不推送凌越
4. 主檔衝突以凌越為準；草稿以外掛為準（寫入凌越的衝突處理留待 Phase 4）
5. 新功能先標註欄位分類（`plugin_only` / `erp_cached` / `erp_live`），再決定讀寫路徑

## 報價狀態（長期設計；推送尚未實作）

| 狀態 | 推送凌越 |
|------|----------|
| DRAFT / SENT / REJECTED | 否 |
| ACCEPTED | 是（**Phase 4，暫不實作**） |
| PUSH_FAILED | 重試（**Phase 4，暫不實作**） |

## 權限

- 外掛獨立帳號（業務／主管／管理員）— 規劃中
- 後端以系統服務帳號呼叫凌越 SOAP 唯讀；業務不需登入凌越

## 實作 Phase

| Phase | 內容 | 狀態 |
|-------|------|------|
| 1 | PostgreSQL + API，localStorage 遷移（無凌越） | 待辦 |
| 2 | 帳號／角色、多人（無凌越） | 待辦 |
| 3 | SOAP **唯讀**同步／查詢主檔 | **進行中**（貨品 `000000` 搜尋已上線） |
| 4 | `ACCEPTED` 推送客戶／貨品／報價單 | **暫緩**；須 Noah 同意後才開始 |

## 開發原則

- 優先 SOAP API 唯讀，不直連凌越 DB
- **禁止**在未進入 Phase 4 前實作凌越寫入、`erp_sync_log` 推送重試、或 `ACCEPTED` 觸發 SOAP 轉入
- 新功能先標註欄位分類，再決定讀寫路徑；現階段預設為唯讀查詢

## 凌越 API 備忘（轉入／轉出代碼不同）

| 資料 | `LyDataOut`（轉出／查詢） | `LyDataIn`（轉入／寫入，暫不使用） |
|------|---------------------------|-----------------------------------|
| 貨品 | `000000` | `000000` |
| 客戶 | `00000D` | `000001` |
| 客戶報價單 | `0000A5` | `0000A5` |
